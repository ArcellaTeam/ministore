# `ministore` — Архитектура

**WAL-based embedded store для append-only журналов сериализуемых записей**

---

### 🎯 Цель

Предоставить **минимальный, надёжный и встраиваемый** механизм для:

1. **Долговечной записи** сериализуемых записей (мутаций, событий, команд) в append-only журнал.
2. **Воспроизведения** всего журнала как потока строго типизированных записей.

> **`ministore` не управляет состоянием.** Он отвечает **только за durability и воспроизведение**. Логика применения записей к состоянию остаётся на стороне вызывающего кода.

---

### 🧩 Ключевые принципы

1. **Простота** — ядро < 150 строк кода, без фоновых задач, компактификации или кэширования.
2. **Надёжность** — каждая запись сопровождается `fsync()`, гарантируя её сохранность при сбое.
3. **Типобезопасность** — записи сериализуются и десериализуются через `serde`.
4. **Человекочитаемость** — журнал хранится в формате JSONL (JSON Lines), легко читается вручную.
5. **Zero runtime overhead** — нет сборщика мусора, фоновых потоков или сложных алгоритмов.

---

### 🏗️ Архитектура

```
┌───────────────────────────┐
│        Приложение         │
├────────────┬──────────────┤
│ Управление │  Записи      │
│ состоянием │  (мутации,   │
│            │   события)   │
└────────────┴──────────────┘
               │
               ▼
┌───────────────────────────┐
│        MiniStore          │
├────────────┬──────────────┤
│   append() │   replay()   │
└────────────┴──────────────┘
               │
               ▼
┌───────────────────────────┐
│      Журнал (WAL)         │
│  (файл в формате JSONL)   │
└───────────────────────────┘
```

- **Журнал**: append-only файл, неизменяемый после записи.
- **`append()`**: добавляет сериализованную запись в конец журнала и выполняет `fsync()`.
- **`replay()`**: читает журнал с диска и возвращает `Vec` десериализованных записей.

---

### 📜 Формат журнала

Журнал представляет собой текстовый файл в формате **[JSONL](http://jsonlines.org/)** со следующей структурой:

```text
// MINISTORE JOURNAL v0.1.0
{"EventType":"UserCreated","user_id":123}
{"EventType":"OrderPlaced","order_id":456}
```

1. **Строка 1**: магическая строка-заголовок, идентифицирующая файл как журнал `ministore` и указывающая его версию.
2. **Строка N (N ≥ 2)**: одна запись в формате JSON, сериализованная из типа `R: Serialize`.

Формат легко отлаживается стандартными Unix-утилитами (`cat`, `tail`, `jq`).

---

### 🔁 Гарантии

| Гарантия | Описание |
|---------|----------|
| **Атомарность** | Каждый вызов `append()` записывает ровно одну запись (одну JSON-строку). |
| **Долговечность** | После возврата `append()` с `Ok(())` запись гарантированно сохранена на диске. |
| **Порядок** | Записи в `replay()` возвращаются в том же порядке, в каком были добавлены. |
| **Безопасность воспроизведения** | При открытии проверяется магический заголовок — предотвращает чтение случайных/битых файлов. |

---

### 📦 API

```rust
impl MiniStore {
    /// Открывает (или создаёт) журнал по указанному пути.
    pub async fn open<P: AsRef<Path>>(path: P) -> Result<Self>;

    /// Добавляет запись в журнал и гарантирует её долговечность (через fsync).
    pub async fn append<R>(&mut self, record: &R) -> Result<()>
    where
        R: Serialize;

    /// Воспроизводит все записи из журнала по указанному пути.
    pub async fn replay<R, P: AsRef<Path>>(path: P) -> Result<Vec<R>>
    where
        R: DeserializeOwned;
}
```

---

### 🚫 Что НЕ делает `ministore`

- **Не управляет состоянием приложения.** Вы сами решаете, как применять записи к своему состоянию.
- **Не поддерживает параллельную запись из коробки.** Для совместного доступа используйте `Arc<Mutex<MiniStore>>` или аналоги.
- **Не обеспечивает высокую производительность записи.** Каждый `append()` делает `fsync` → медленно, но надёжно.
- **Не поддерживает удаление или компактификацию журнала.** Это задача для приложения (например, создание снапшотов).

---

### 💡 Типичные сценарии использования

- **Event Sourcing**: сохранение истории изменений доменной модели.
- **Управляющие журналы**: durable-логи для метаданных, конфигурации, реестров.
- **Audit trails**: неизменяемые журналы операций для безопасности и отладки.

> `ministore` — это **строительный блок**, а не полноценная база данных. Он идеален там, где важна **простота и надёжность**, а не производительность или сложные запросы.